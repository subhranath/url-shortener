A Django application to tryout the 'url_shortify' application.

MySQL Database Setup
====================
create database url_shortener character set utf8;
create user 'url_shortener'@'localhost' identified by 'url_shortener';
grant all on url_shortener.* to 'url_shortener'@'localhost';
